import React from 'react';
import { ScrollView, Text } from 'react-native';

const NoticeItemScreen: React.FC = () => {
  return (
    <ScrollView>
      <Text>NoticeItemScreen</Text>
    </ScrollView>
  );
};

export default NoticeItemScreen;
